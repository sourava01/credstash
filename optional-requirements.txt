# Optional Dependencies
# To install, run:
#      $ pip install -r optional-requirements.txt

# In order to output `getall` in YAML format you need PyYAML
# pytest is needed to run the tests in test_credstash_lib.py
pyyaml==6.0.2
pytest==5.4.1